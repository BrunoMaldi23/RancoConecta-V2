import 'package:flutter/material.dart';

abstract final class RancoColors {
  static const primaryDark = Color(0xFF145A3A);
  static const primary = Color(0xFF2F7D57);
  static const primarySoft = Color(0xFFEAF5EF);
  static const primaryMuted = Color(0xFFDDEFE7);
  static const surface = Color(0xFFF7FBF8);
  static const surfaceElevated = Colors.white;
  static const border = Color(0xFFD6E6DE);
  static const textPrimary = Color(0xFF183027);
  static const textSecondary = Color(0xFF64786F);
  static const success = Color(0xFF23875B);
  static const error = Color(0xFFB94A42);
  static const warning = Color(0xFF8A6A1F);
  static const info = Color(0xFF2D6F72);

  static const forest = primary;
  static const pine = primaryDark;
  static const lake = info;
  static const moss = Color(0xFF5E7F3A);
  static const mist = primarySoft;
  static const canvas = surface;
  static const ink = Color(0xFF17211D);
  static const slate = textSecondary;
  static const night = Color(0xFF101918);
}
